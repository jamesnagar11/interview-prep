import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-sans",
  display: "swap",
});

export const metadata: Metadata = {
  title: "PrepKit — AI Interview Preparation",
  description:
    "Turn any job description into a personalised interview preparation kit in minutes. Company brief, question bank, flashcards, and a study schedule — all AI-generated.",
  keywords: ["interview prep", "AI", "job description", "flashcards", "study schedule"],
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className={`${inter.variable} dark`} suppressHydrationWarning>
      <body className="min-h-screen bg-background text-foreground antialiased">
        {children}
      </body>
    </html>
  );
}
